#include <raylib.h>

#include "maze.h"

int main() {
    
    // InitWindow(100, 100, "Hmmf Mazes");
    // SetTargetFPS(120);

    Maze maze(21, 21, 20, 4);
    maze.Start();

    // while (!WindowShouldClose()){
        
    // }
}